---
import MainLayout from "../layouts/MainLayout.astro";
import { content } from "../data/content";

const t = content.fr;
---

<MainLayout title="Arthur K. | Solutions Engineer & Architecte SI">
    
    <section id="profil" class="min-h-[85vh] flex flex-col justify-center items-center text-center pt-10">
        
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-green-500/10 border border-green-500/30 w-fit mb-8 hover:bg-green-500/20 transition-colors cursor-default backdrop-blur-sm">
            <span class="relative flex h-3 w-3">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span>
            </span>
            <span class="text-green-400 text-xs md:text-sm font-mono font-bold tracking-wide uppercase">
                {t.hero.badge}
            </span>
        </div>

        <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 leading-tight font-mono tracking-tight text-balance">
            {t.hero.title}<br />
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-cyan-400">
                {t.hero.subtitle}
            </span>
        </h1>
     <p class="text-lg md:text-2xl text-slate-300 max-w-4xl leading-relaxed mb-10 mx-auto px-6 text-center text-balance">
            Issu de la protection de la nature, je me forme à l'architecture logicielle pour mettre la technique au service de l'environnement. 
            Ma démarche est guidée par une volonté précise : 
            <strong class="text-white font-bold block md:inline mt-2 md:mt-0">concevoir un numérique utile et durable</strong>.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center w-full px-4">
            <a href="#projets" class="bg-gradient-to-r from-green-500 to-cyan-500 text-[#0a0f1c] px-8 py-3 rounded-lg font-bold hover:shadow-[0_0_20px_rgba(56,189,248,0.4)] hover:scale-105 transition-all text-center">
                {t.hero.cta_projects}
            </a>
            <a href="/Portfolio---AK/assets/CV_Arthur_KANDYBOWICZ.pdf" target="_blank" class="flex items-center justify-center gap-2 border border-slate-600 text-slate-300 px-8 py-3 rounded-lg font-bold hover:border-white hover:text-white hover:bg-white/5 transition-all">
                <span class="text-lg">📄</span>
                {t.hero.cta_cv}
            </a>
        </div>
    </section>

    <section id="parcours" class="mb-24 scroll-mt-24 max-w-4xl mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-3xl font-bold text-white mb-2">Formations & Diplômes</h2>
        </div>

        <div class="space-y-8 relative before:absolute before:inset-0 before:ml-5 before:-translate-x-px md:before:mx-auto md:before:translate-x-0 before:h-full before:w-0.5 before:bg-gradient-to-b before:from-transparent before:via-slate-700 before:to-transparent">
            {t.timeline.map((item) => (
                <div class="relative flex items-center justify-between md:justify-normal md:odd:flex-row-reverse group">
                    <div class="flex items-center justify-center w-10 h-10 rounded-full border border-slate-700 bg-[#0a0f1c] group-hover:border-cyan-400 shadow z-10 shrink-0 md:order-1 md:group-odd:-translate-x-1/2 md:group-even:translate-x-1/2 transition-colors">
                        <div class="w-2 h-2 rounded-full bg-slate-500 group-hover:bg-cyan-400" />
                    </div>
                    <div class="w-[calc(100%-4rem)] md:w-[calc(50%-2.5rem)] p-6 rounded-xl border border-slate-800 bg-[#111a2d]/80 backdrop-blur-sm hover:border-slate-600 transition-all hover:-translate-y-1">
                        <div class="flex flex-col mb-2">
                            <div class="flex justify-between items-start">
                                <span class="font-bold text-white text-lg leading-tight">{item.title}</span>
                                <span class="font-mono text-[10px] text-cyan-400 border border-cyan-400/20 px-2 py-1 rounded bg-cyan-950/30 whitespace-nowrap ml-2">
                                    {item.year}
                                </span>
                            </div>
                            <span class="text-sm font-mono text-slate-500 mt-1">{item.place}</span>
                        </div>
                        <p class="text-slate-400 text-sm leading-relaxed">
                            {item.desc}
                        </p>
                    </div>
                </div>
            ))}
        </div>
    </section>

    <section id="competences" class="mb-24 scroll-mt-24 max-w-5xl mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-3xl font-bold text-white mb-2">Compétences</h2>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div class="bg-[#111a2d]/60 backdrop-blur-md p-6 rounded-xl border border-slate-800 hover:border-cyan-500/40 transition-colors h-full flex flex-col">
                <h3 class="font-mono text-cyan-400 mb-4 border-b border-slate-700/50 pb-2 text-center">Architecture & Back</h3>
                <div class="flex flex-wrap gap-2 justify-center">
                    {t.skills.stack.map((s) => (
                        <span class="px-3 py-1 bg-slate-900/50 text-slate-300 text-xs font-mono rounded-lg border border-slate-700/50 hover:text-white hover:border-cyan-500/30 transition-all cursor-default">{s}</span>
                    ))}
                </div>
            </div>
            <div class="bg-[#111a2d]/60 backdrop-blur-md p-6 rounded-xl border border-slate-800 hover:border-green-500/40 transition-colors h-full flex flex-col">
                <h3 class="font-mono text-green-400 mb-4 border-b border-slate-700/50 pb-2 text-center">Web & Green IT</h3>
                <div class="flex flex-wrap gap-2 justify-center">
                    {t.skills.web.map((s) => (
                        <span class="px-3 py-1 bg-slate-900/50 text-slate-300 text-xs font-mono rounded-lg border border-slate-700/50 hover:text-white hover:border-green-500/30 transition-all cursor-default">{s}</span>
                    ))}
                </div>
            </div>
            <div class="bg-[#111a2d]/60 backdrop-blur-md p-6 rounded-xl border border-slate-800 hover:border-purple-500/40 transition-colors h-full flex flex-col">
                <h3 class="font-mono text-purple-400 mb-4 border-b border-slate-700/50 pb-2 text-center">Outils & Méthodes</h3>
                <div class="flex flex-wrap gap-2 justify-center">
                    {t.skills.tools.map((s) => (
                        <span class="px-3 py-1 bg-slate-900/50 text-slate-300 text-xs font-mono rounded-lg border border-slate-700/50 hover:text-white hover:border-purple-500/30 transition-all cursor-default">{s}</span>
                    ))}
                </div>
            </div>
        </div>
    </section>

    <section id="projets" class="mb-24 scroll-mt-24 max-w-5xl mx-auto px-4">
        <div class="text-center mb-12">
            <h2 class="text-3xl font-bold text-white mb-2">Projets & Réalisations</h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {t.projects.map((p) => {
                const isLink = p.url && p.url !== '#';
                const Tag = isLink ? 'a' : 'div';
                const hrefProps = isLink ? { href: p.url, target: "_blank", rel: "noopener noreferrer" } : {};
                const cursorClass = isLink ? 'cursor-pointer hover:-translate-y-1 hover:shadow-lg' : 'cursor-default opacity-90';

                return (
                <Tag {...hrefProps} class={`flex flex-col p-6 rounded-2xl border bg-[#111a2d]/80 backdrop-blur-sm transition-all duration-300 ${p.color} border-opacity-30 group relative h-full min-h-[240px] ${cursorClass}`}>
                    
                    {isLink && (
                        <div class="absolute top-5 right-5 opacity-0 group-hover:opacity-100 transition-opacity text-slate-400 bg-slate-900/50 p-1 rounded">
                            ↗
                        </div>
                    )}
                    
                    <div class="flex justify-between items-start mb-4 pr-8">
                        <span class="text-[10px] font-mono font-bold text-slate-500 uppercase tracking-widest border border-slate-800 px-2 py-1 rounded bg-[#0a0f1c]">{p.type}</span>
                        <span class={`text-[10px] px-2 py-0.5 rounded border ${p.status.includes('Roadmap') ? "border-purple-500 text-purple-400 bg-purple-500/10" : p.status.includes('Académique') ? "border-red-500 text-red-400 bg-red-500/10" : "border-green-500 text-green-400 bg-green-500/10"}`}>
                            {p.status}
                        </span>
                    </div>

                    <h3 class="text-xl font-bold text-white mb-3 group-hover:text-cyan-400 transition-colors flex items-center gap-2">
                        {p.name}
                    </h3>

                    <p class="text-sm text-slate-400 mb-6 flex-1 line-clamp-4 leading-relaxed">
                        {p.desc}
                    </p>

                    <div class="flex gap-2 flex-wrap mt-auto pt-4 border-t border-slate-800/50">
                        {p.tags.map((tag) => (
                            <span class="text-[10px] font-mono px-2 py-1 bg-slate-900/80 rounded text-slate-400 border border-slate-800 group-hover:border-slate-700 transition-colors">{tag}</span>
                        ))}
                    </div>
                </Tag>
            )})}
        </div>
    </section>

    <section id="alternance" class="mb-24 max-w-4xl mx-auto px-4">
        <div class="bg-gradient-to-b from-[#111a2d] to-[#0d1321] border border-slate-700/50 rounded-3xl p-8 md:p-12 text-center relative overflow-hidden shadow-2xl">
            <div class="absolute top-0 right-1/2 translate-x-1/2 w-64 h-64 bg-green-500/10 rounded-full blur-[80px] pointer-events-none"></div>

            <h3 class="text-2xl md:text-3xl font-bold text-white mb-3 relative z-10">{t.recruiter.title}</h3>
            <p class="text-slate-400 mb-10 font-mono text-sm relative z-10 uppercase tracking-widest opacity-80">{t.recruiter.subtitle}</p>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-10 relative z-10">
                {t.recruiter.rhythm.map((r) => (
                    <div class="bg-[#0a0f1c]/80 backdrop-blur border border-slate-800 p-4 rounded-xl hover:border-green-500/30 transition-colors">
                        <div class="text-green-400 font-bold font-mono mb-1 text-lg">{r.year}</div>
                        <div class="text-slate-400 text-xs md:text-sm">{r.detail}</div>
                    </div>
                ))}
            </div>

            <div class="flex flex-col sm:flex-row justify-center gap-4 relative z-10">
                <a href="/Portfolio---AK/assets/Calendrier_Promo2028.pdf" download class="flex items-center justify-center gap-2 bg-slate-800 hover:bg-slate-700 text-white px-8 py-3 rounded-lg font-bold transition-all text-sm w-full sm:w-auto border border-slate-600">
                    📅 {t.recruiter.calendar_btn}
                </a>
                <a href="/Portfolio---AK/assets/CV_Arthur_KANDYBOWICZ.pdf" download class="flex items-center justify-center gap-2 bg-green-600 hover:bg-green-500 text-white px-8 py-3 rounded-lg font-bold transition-all text-sm w-full sm:w-auto shadow-lg hover:shadow-green-500/20">
                    📄 {t.recruiter.cv_btn}
                </a>
            </div>
            
            <div class="mt-8 text-xs md:text-sm text-slate-500 relative z-10 font-mono">
                📍 Mobilité : <span class="text-slate-300">{t.recruiter.mobility}</span>
            </div>
        </div>
    </section>

    <section id="contact" class="py-12 max-w-xl mx-auto text-center px-4">
        <h2 class="text-3xl font-bold text-white mb-4">Contact</h2>
        <p class="text-slate-400 mb-8">Un projet ? Une opportunité ? Discutons-en.</p>

        <form action="https://formspree.io/f/myzvagnw" method="POST" class="space-y-4 text-left">
            <div class="grid grid-cols-1 gap-4">
                <div class="relative">
                    <input type="text" name="name" required placeholder="Nom / Entreprise" class="w-full bg-[#111a2d] border border-slate-700 rounded-lg p-4 pl-4 text-white focus:border-cyan-400 focus:ring-1 focus:ring-cyan-400 focus:outline-none transition-all placeholder:text-slate-600" />
                </div>
                <div class="relative">
                    <input type="email" name="email" required placeholder="votre@email.com" class="w-full bg-[#111a2d] border border-slate-700 rounded-lg p-4 pl-4 text-white focus:border-cyan-400 focus:ring-1 focus:ring-cyan-400 focus:outline-none transition-all placeholder:text-slate-600" />
                </div>
            </div>
            <textarea name="message" rows="5" required placeholder="Détails de la mission..." class="w-full bg-[#111a2d] border border-slate-700 rounded-lg p-4 text-white focus:border-cyan-400 focus:ring-1 focus:ring-cyan-400 focus:outline-none transition-all placeholder:text-slate-600 resize-none"></textarea>
            
            <button type="submit" class="w-full bg-gradient-to-r from-green-500 to-cyan-500 text-[#0a0f1c] font-bold py-4 rounded-lg hover:shadow-lg hover:scale-[1.01] transition-all uppercase tracking-widest text-sm">
                Envoyer le message
            </button>
        </form>
    </section>

</MainLayout>